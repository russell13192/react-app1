import React from 'react';

const useroutput = (props) => {
    return (
        <div className="UserOutput">
            <p>{props.username}</p>
            <p>Tummy!!!</p>
        </div>
    )
};

export default useroutput;